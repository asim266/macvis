export function MCPs() {
  return (
    <div className="flex flex-col h-full" style={{ padding: 32 }}>
      <h2 style={{ fontSize: 18, fontWeight: 600, color: 'var(--text-primary)', marginBottom: 8 }}>MCP Connections</h2>
      <p style={{ fontSize: 13, color: 'var(--text-muted)' }}>MCP manager coming in Phase 4. Configure developer platform integrations here.</p>
    </div>
  )
}
